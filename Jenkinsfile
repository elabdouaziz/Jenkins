pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Ajoutez ici des commandes de build, comme 'make build' ou 'npm install'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing...'
                // Ajoutez ici des commandes de test, comme 'make test' ou 'npm test'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Ajoutez ici des commandes de déploiement, comme 'scp' ou 'docker push'
            }
        }
    }
}
